import styled from 'styled-components';

const StyledPreTitle = styled.h3`
    font-family: var(--font-cursive), cursive;
    font-size: clamp(5rem, 8vw, 10rem);
    text-align: center;
    opacity: 0.25;
    color: var(--color-primary);
    line-height: 120%;
    font-weight: 100;
    margin-bottom: -3rem;
    position: relative;
    z-index: -1;
`;
export default StyledPreTitle;
